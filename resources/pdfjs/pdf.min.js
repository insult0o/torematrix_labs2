// PDF.js v3.11.x - Production Build
// This is a placeholder file. In production, download the actual PDF.js library from:
// https://github.com/mozilla/pdf.js/releases/tag/v3.11.174

// Minimal PDF.js API simulation for development
if (typeof pdfjsLib === 'undefined') {
    window.pdfjsLib = {
        GlobalWorkerOptions: {
            workerSrc: 'pdf.worker.min.js'
        },
        getDocument: function(src) {
            return {
                promise: Promise.reject(new Error('PDF.js library not loaded. Please download the actual PDF.js files from https://github.com/mozilla/pdf.js/releases'))
            };
        },
        version: '3.11.174',
        build: 'placeholder'
    };
}

console.warn('PDF.js placeholder loaded. Download actual PDF.js library for production use.');